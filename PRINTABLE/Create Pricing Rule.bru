meta {
  name: Create Pricing Rule
  type: http
  seq: 30
}

post {
  url: http://localhost:5000/api/service/pricing
  body: json
  auth: inherit
}

body:json {
  {
    "id": "pr_1",
    "merchantServiceId": "ms_1",
    "price": 0.20,
    "attributes": { "paper_size": "A4" }
  }
}
